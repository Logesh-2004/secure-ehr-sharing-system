# Clinician App

Flutter app for clinician authentication, CQEP QR scanning, token validation, and access logging.

Before running, register this app in the same Firebase project used by `patient_app` and replace platform Firebase configuration files, including `android/app/google-services.json`.
